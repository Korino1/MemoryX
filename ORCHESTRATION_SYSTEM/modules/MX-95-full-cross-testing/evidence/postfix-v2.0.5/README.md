# MX-95 Post-Fix Evidence Bundle

Run ID: `20260802T062756339Z`

This bounded bundle contains the machine-checkable evidence needed to join the
authoritative 47-tool surface to direct cases, cross-tool sequences,
resilience, determinism, and fail-closed validator controls. Raw process logs
and disposable database files remain local and are intentionally excluded.

`coverage-ledger.json`, `calls.jsonl`, and `sequences.json` must be validated
together. `surface.json` is the authoritative tool inventory for this run.
`postfix-run-summary.json` records the checked 2.0.5 binary identity and the
explicit limitations. Structural recovery tests do not close N5.
