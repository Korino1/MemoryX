# Task

Status: `MX95_LIVE_CONTOUR_ACCEPTED`

Create a permanent MemoryX-native modular orchestration system from the
canonical concept and active roadmap. Materialize a root registry and eleven
non-overlapping autonomous module contours with immutable
`gpt-5.6-sol`/`xhigh` execution, real-only persistent session binding, compact
recovery contracts, strict module-local MemoryX contracts, separate repo-local
physical bases, EvidenceReturn, shared-host coordination, and validators.

Acceptance boundary:

- preserve all MemoryX concept invariants and the open N5 roadmap state;
- never synthesize session UUIDs or use user/foreign bases;
- validate structure and physical base layout without claiming unobserved live
  Codex hook, compact, cache, or model-quality behavior;
- preserve the pre-existing dirty 2.0.3 candidate and `Исправить.txt`.

Final checkpoint evidence also includes direct execution of all twelve
SessionStart scripts and an acquire/release smoke of the shared-host resource
lease. Neither is treated as a real Codex lifecycle event.

MX-95 was activated through the official wrapper with real session
`019fc0f9-f099-79a2-be80-ebe515628fa5`, then resumed for a post-fix 2.0.5 audit.
This proves that module's observed invocation and test evidence only; it does
not prove platform compact hooks, cache reuse, model quality, total semantic
acceptance, or N5 completion.
