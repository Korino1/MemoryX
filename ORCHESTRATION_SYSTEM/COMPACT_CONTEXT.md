# Compact Context

Module: `MX-ROOT` / `root-orchestrator`
Session: `UNBOUND`
Model: `gpt-5.6-sol`; reasoning: `xhigh`; `max` forbidden
Task state: English-only inter-agent contract validated

Current durable state:

- `manifest.json` routes eleven autonomous modules; `session_registry.json`
  mirrors the actual binding files.
- Twelve contours have strict contracts, hooks, dossiers, EvidenceReturn, and
  separate project-local bases.
- MX-95 is bound to `019fc0f9-f099-79a2-be80-ebe515628fa5`; every other
  contour is `UNBOUND`.
- MX-95 run `20260802T062756339Z` passed its 47-tool direct/cross and post-fix
  determinism gates for candidate 2.0.5.
- `INTER_AGENT_COMMUNICATION.md` is authoritative. The wrapper validates task
  prompts and stable prefixes; generated contours inherit the marker,
  EvidenceReturn declaration, recovery declaration, and canonical hook copies.
- The focused language gate passed 12 contours and 203 persisted files; all
  seven fail-closed controls passed without starting Codex or touching a
  foreign process.
- `run_all_validations.ps1 -RequireBases` passed for all 12 contours with
  11 manifest modules, exact session-registry agreement, and zero failures.
- N5 remains open. No real compact/resume, cache reuse, or model-quality proof
  exists.

Recovery order:

1. `ARCHITECTURE.md` and `manifest.json`
2. `CANONICAL_PACKET.md`
3. `TASK.md`, `PLAN.md`, and `PROGRESS.md`
4. `DECISIONS.md` and `ACCEPTANCE.md`
5. `state/RECOVERY.json` and `MEMORYX_CONTRACT.json`
6. selected module's same contour files and dossiers

This is saved context, not evidence that a real compact event occurred.
